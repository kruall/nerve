# example-mcp

Copy this into a standalone repository, rename the package, and generate its
lockfile (for example `uv lock`). The server deliberately has no Nerve runtime
dependency. Run `python -m unittest discover -s tests -v` before registration.
